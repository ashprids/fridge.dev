This feed post tests **bold**, *italic*, **bold with *nested italic***, <u>underline</u>, ~~strikethrough~~, ==highlighting==, and ||spoiler text||.

Inline code keeps syntax literal: `**not bold**` and `!fa solid code`.

[External link](https://example.com) and [internal link](/feed).

![Image description](/resources/favicon.svg)

> This is a blockquote.
> It contains **bold**, *italic*, `inline code`, and ||a spoiler||.

- Unordered item
- Another unordered item
    - Nested unordered item
    1. Nested ordered item

1. First ordered item
2. Second ordered item
    1. Nested ordered item
    2. Another nested ordered item

```
const greeting = "hello";
console.log(greeting);
```

| Element | Example |
| --- | --- |
| Bold | **Important** |
| Italic | *Emphasis* |
| Code | `example()` |
| Spoiler | ||Hidden|| |
| Link | [Open](/feed) |

Icons: !fa solid code !fa regular heart !fa brands github !frdg

Use the feed toolbar to attach an image, an audio file, a video file, and a recorded voice note beneath this paragraph. Confirm that each temporary placeholder is replaced after posting.
